/*
 * Print the number in the integer, octal and hexadecimal representation
 */

#include "stdio.h"

int main()
{
	int a = 15;
	printf("a integer = %i\n",a );
	printf("a octal = %o\n", a );
	printf("a hexadecimal = %X\n",a );
	return 0;
}
