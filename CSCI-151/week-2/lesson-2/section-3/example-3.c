/*
 * print a number in decimal, octal and hexadecimal representations
 */

#include <stdio.h>

int main()
{
	int a = 15;
	printf("a integer %i\n", a);
	printf("a octal %o\n", a);
	printf("a hexadecimal %X\n", a);

	return 0;
}
